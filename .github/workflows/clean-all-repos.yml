# .github/workflows/auto-cleanup.yml
name: Auto Cleanup Workflow Runs
on:
  # 每周一凌晨3点自动运行
  schedule:
    - cron: '0 3 * * 1'
  # 允许手动触发
  workflow_dispatch:
    inputs:
      keep_count:
        description: '保留数量'
        required: false
        default: '30'

permissions:
  actions: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Clean all repositories
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ secrets.OTHER_REPO_TOKEN }}
          repository: '*'  # 所有仓库
          keep_minimum_runs: ${{ inputs.keep_count || 30 }}
          delete_run_by_conclusion_pattern: 'success,failure'
      
      - name: Send notification
        if: always()
        run: |
          echo "### 🧹 工作流记录清理完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **触发方式**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **保留数量**: ${{ inputs.keep_count || 30 }} 条/工作流" >> $GITHUB_STEP_SUMMARY
          echo "- **执行时间**: $(date)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "所有仓库的工作流记录已清理完成。" >> $GITHUB_STEP_SUMMARY
